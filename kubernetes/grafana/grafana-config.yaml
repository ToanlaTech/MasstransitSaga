apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-config
  namespace: default
data:
  grafana.ini: |
    [server]
    protocol = http
    http_port = 3000
    domain = grafana.local

    [auth.anonymous]
    enabled = true
